SELECT Name, SUBSTRING(name, 1, 1) AS FirstLetter ,  --Prints first letter in each row of Name column  
SUBSTRING(Name, 2, 3) AS SecondThirdAndFourthLetter  -- Prints 3 letters, starting from secound letter in each row of Name column.
FROM SalesLT.Product
ORDER BY Name                                        --Orders by Name ascending
